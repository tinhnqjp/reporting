'use strict';

var m_foods = require('../controllers/mobiles/foods-mobile.server.controller');


module.exports = function (app) {

  // app.route('/api/mobile/auth/signin').post(m_foods.m_signin);

  // app.route('/api/mobile/auth/password').post(m_foods.m_password);

  // app.route('/api/mobile/auth/registry').post(m_foods.m_registry);
};
